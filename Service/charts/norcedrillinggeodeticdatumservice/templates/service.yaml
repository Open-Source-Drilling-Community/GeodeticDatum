apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillinggeodeticdatumservice.fullname" . }}
  labels:
    {{- include "norcedrillinggeodeticdatumservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillinggeodeticdatumservice.selectorLabels" . | nindent 4 }}
